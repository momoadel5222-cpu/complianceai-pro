services:
  - type: web
    name: complianceai-backend
    env: python
    plan: free
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && python flask_backend.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
